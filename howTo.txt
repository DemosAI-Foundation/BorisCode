/loop — Schedule Recurring Tasks
Concept: Schedule recurring jobs (babysit PRs, summarize Slack) locally.
OpenCode Automation Plan:
Create a Plugin that adds a looping mechanism right into OpenCode via the SDK.


ctrl+b Mid-task side-chain questions without breaking flow.

/teach command that utilizes the webfetch tool and ascii diagramming.

/commands for pr push